interface WrapperProps {
	padding: string;
	margin: string;
	height: string;
	width: string;
	border: string;
	zIndex: number;
}

interface SvgProps {
	height: string;
	width: string;
	color: string;
}

export type { WrapperProps, SvgProps };
